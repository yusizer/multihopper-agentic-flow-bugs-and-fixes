# MultiHopper API — DEVNET ONLY
MH_API_BASE=https://devnet.multihopper.com/api/v1
MH_API_KEY=mh_test_REPLACE_ME

# Solana signing wallet (base58 private key) — funded on devnet
SOLANA_PRIVATE_KEY=BASE58_PRIVKEY_REPLACE_ME
RPC_URL=https://api.devnet.solana.com

# Optional SPL token for angle D (non-SOL / Token-2022 testing)
SPL_MINT=
SPL_DECIMALS=6

# Webhook receiver (angle B) — ngrok URL
WEBHOOK_PUBLIC_URL=
WEBHOOK_RECEIVER_PORT=8765
WEBHOOK_SECRET=
